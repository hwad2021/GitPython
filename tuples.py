#TUPLE are immutable
numbers = (1,2,3,3,3 ) #difference between tuple and list is the different brackets are used
#numbers[0] = 1 #assginment won't work as tuple doesn't support assignment
numbers.count(3) #no append, insert or clear method 
numbers.index(self, x)